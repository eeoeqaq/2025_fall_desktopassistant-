
#include"shouzhi.h"


/*

recordings::recordings(string new_name,long long new_amount,iotype typea,detail_type typeb):
    name(new_name),amount(new_amount),type1(typea),type2(typeb){};

void irecordings::write (){
    ofstream ofs;
    ofs.open("test.txt",ios::out|ios::ate|ios::binary);
    ofs.write((char*)this,sizeof(*this));
    ofs.close();
};

void irecordings::read (){
    ifstream ifs;
    ifs.open("test.txt",ios::in|ios::ate|ios::binary);
    ifs;
                    
};*/